#include "GameState.hh"

int main(int, char*[]) {
	srand(time(NULL)); // Allows the rand() to work correctly




	//SM.rank = new RankScene();
	//for (int i = 0; i < RANK_LEN; ++i) {
	//	PlayerData tmp{10, "Pardal"};
	//	SM.rank->AddSetValue(tmp);
	//}
	//SM.rank->UpdateRank();

	GameState(800, 800);

	return 0;
}



